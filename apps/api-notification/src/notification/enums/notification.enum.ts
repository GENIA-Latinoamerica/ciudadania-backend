export enum NotificationEnum {
    CANCELLED = 'cancelled',//Your booking has been cancelled by either you or the service provider.
    CONFIRMED = 'confirmed',//The user was marked as present in the place.
}